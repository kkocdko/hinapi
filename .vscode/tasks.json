{
  "version": "2.0.0",
  "tasks": [
    {
      "label": "run",
      "type": "shell",
      "command": "cmake -B build -G Ninja && cmake --build build && node src/main.js",
      "group": { "kind": "build", "isDefault": true }
    }
  ]
}
